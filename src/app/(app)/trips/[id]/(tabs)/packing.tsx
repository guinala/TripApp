import { Text, View } from 'react-native';

export default function PackingScreen() {
  return (
    <View style={{ flex: 1, alignItems: 'center', justifyContent: 'center' }}>
      <Text>DiaryScreen (paso 4)</Text>
    </View>
  );
}
